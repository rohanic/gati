Real app captures go here — 1080×1920, PNG, named in display order:

  01-today.png  02-numbers.png  03-number-detail.png
  04-wander.png  05-story.png   06-unlock.png

Capture with:  adb shell screencap -p /sdcard/s.png && adb pull /sdcard/s.png

Do not generate these with an image model. Play's metadata policy requires
screenshots to show actual app content; invented screens risk rejection and,
once live, a takedown.
